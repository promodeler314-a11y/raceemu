# サーバ側で探索を回す設計

自前の k3s クラスタで組み合わせ探索を走らせ、結果を画面で受ける。
Cloudflare Tunnel 越しに外部公開し、狭いコミュニティで使う前提である。

## 1. なぜサーバに出すのか

いまの探索の候補は「利用者が選んでいるスキル」に固定されている（`apps/web/src/store.ts` の `candidates`）。
数百あるスキル全体から予算内の最良を選ぶ、という本来の問いには答えていない。
これは設計の判断ではなく、ブラウザで回すという制約から来ている。

候補 20 個のいまと、全スキルを候補にした場合の規模を並べる。

| | 候補 20 個 | 候補 200 個 |
| --- | ---: | ---: |
| 単体評価 | 4,000 レース | 40,000 レース |
| 局所探索 1 巡 | 約 35,000 レース | 約 160,000 レース |

スマホの数コアで回す量ではない。
**サーバに出して初めて、いまは答えられていない問いに答えられる。**

[逆算と組み合わせ探索の設計](solver-design.md)の 3.6 節に挙げた予算曲線と採用頻度も、探索を何本も走らせる必要があるため同じ理由で入っていない。

## 2. 譲らないこと：サーバが無くても動く

アプリはいまクライアント側だけで完結している。
**API は加速装置であって、依存先にはしない。**

理由が三つある。

- 静的配信だけの版が生き続ける。クラスタを持たない人にも渡せる。
- クラスタが落ちている間も使える。
- 配布物が自己完結していれば、AGPL の下で渡すものが「アプリ一式」で済む。

実装としては、API の宛先が設定されていなければ今までどおりブラウザの Worker で回す。
設定されていれば、そちらに投げる選択肢が画面に出る。

## 3. 形

### 3.1 ジョブにする

1 本の探索が 25 万レース規模になる。
分単位でかかるので、HTTP の要求と応答を往復させる形では持たない。

- `POST /api/search` が受け付けてジョブ ID を返す。
- `GET /api/search/{id}` が進捗と、終わっていれば結果を返す。
- `DELETE /api/search/{id}` で中断する。

`optimizeSkills` は既に `onProgress(message)` と `AbortSignal` を受け取るので、この三つはそのまま対応する。

### 3.2 出入りの型はもう在る

**新しく定義する型がほとんど無い。**

要求は `OptimizeContext` と `OptimizeOptions` のうち値だけのもので、`base` は `SerializableRaceSetting`、つまり Worker に送るために既にプレーンな値へ落としてある形である。

応答は `OptimizeResult` がそのまま JSON になる。
型付き配列（`Float64Array` と `Uint8Array`）は `Evaluation` の内側だけにあり、`OptimizeResult` からは外に出ていない。

つまりサーバ側は `WorkerPool` を `nodeWorkerFactory` で作り、既存の `optimizeSkills` をそのまま呼ぶ薄い包みになる。
**計算モデルにも探索にも手を入れない。**

### 3.3 走らせ方

**単一の Deployment に面内のキューを持たせる。**
1 探索につき Kubernetes の Job を 1 個立てる形は採らない。
イメージ取得と起動の待ちが毎回乗るだけで、この規模では得が無い。

ポッドの中で `nodeWorkerFactory` の `WorkerPool` を 1 つ持ち、キューから 1 本ずつ流す。

## 4. 実際に転ぶところ

### 4.1 コンテナの CPU 上限が Node から見えない

`nodeWorkerFactory` の並列数は `os.availableParallelism() - 1` である。
これが見ているのは CPU アフィニティ（`sched_getaffinity`）であって、Kubernetes の `resources.limits.cpu` が設定する CFS のクォータではない。

**16 コアのノードに `limits.cpu: 2` で置いたポッドは、16 と答える可能性が高い。**
Worker が 15 本立ち、2 コアぶんの枠を奪い合って全体が遅くなる。

並列数は環境変数で渡す。
`WorkerPool` のコンストラクタは既に第 2 引数で並列数を受け取れるので、追加の作りは要らない。

入れる前に、ポッドの中で実際に何が返るかを測る。
`apps/web/e2e/smoke.mjs` がブラウザの場所を決め打ちしていて CI で落ちたのと同じ種類の誤りである。

### 4.2 tsx が実行時に要る

`packages/sim/src/parallel/node-worker-bootstrap.mjs` は `tsx/esm/api` を登録してから TypeScript の本体を読み込んでいる。
Node の型剥がしがパラメータプロパティを扱えないためである。

いま `tsx` は devDependency なので、そのままの構成でイメージを作ると Worker が起動しない。
実行時の依存に移すか、サーバ向けにはあらかじめコンパイルする。

### 4.3 上限が無いと 1 人で埋まる

1 本 25 万レースを、数コアのクラスタで回す。
外部公開する以上、次の三つは入れる。

- 同時に走らせるジョブの数（まずは 1）
- 待ち行列の長さの上限。超えたら受け付けを断る
- 1 ジョブあたりのレース数の上限。候補数と予算と試行回数から見積もって弾く

## 5. 公開の仕方

Cloudflare Tunnel が既に通っているので、TLS と経路はそちらに任せる。

**認証は Cloudflare Access を前に置く。**
アプリにもサーバにも認証の実装を持たない。
狭いコミュニティに配るのであれば、これで足りる。

**アプリと API を同一オリジンで配る。**
別オリジンにすると CORS の設定が要り、平文で出そうとすると mixed content で落ちる。
同じホスト名の下に静的ファイルと `/api` を並べれば、どちらも起きない。

## 6. AGPL

**ここで初めて 13 条が実際に効く。**
自分だけで動かしている間は関係なかったが、他人にネットワーク越しに使わせるなら、その人がソースを取得できる必要がある。

フッタのリンクは既にある（`apps/web/src/App.tsx`）。
ただし**動かしている版と push してある版がズレると、リンクがあっても意味がない。**
自前ホストは手で入れ替えられるぶんズレやすい。

そこでビルド時にコミットの SHA を埋め、フッタに出す。
見ている人が、いま動いているものに対応するソースを特定できる状態にする。

## 7. 口の形

`apps/api` に実装した。

| | |
| --- | --- |
| `POST /api/search` | 受け付けてジョブ ID を返す（202）。混んでいれば 503 |
| `GET /api/search/{id}` | 状態・進捗・レース数・結果 |
| `DELETE /api/search/{id}` | 中断する |
| `GET /api/health` | 並列数とその決め方、混み具合 |
| `GET /api/individuals` | 保存した個体（ステータス+スキル構成）の一覧 |
| `POST /api/individuals` | 個体を 1 件保存する |
| `DELETE /api/individuals/{id}` | 個体を 1 件消す |
| `POST /api/ocr/skills` | ウマ娘詳細画面の写真から所持スキルを読む |
| `POST /api/ocr/status` | 同じ写真の上半分からステータスと適性を読む |

探索だけの口として始めたが、ブラウザに置けないものがここに集まっている。
読み取り（`/api/ocr/*`）は[設計](ocr-design.md)のとおり、学習データ 35 MB と
分類のモデルを抱えるためにサーバ側へ出したものである。
`/api` が無い版ではどちらも使えないので、画面はその旨を伝えて倒れる（同 1 節）。

要求は次の形である。
`base` は候補を 1 つも取らない土台で、Worker に送るのと同じ `SerializableRaceSetting` をそのまま使う。

```json
{
  "base": { "uma": {...}, "track": {...}, "skillIds": [] },
  "candidates": ["201112", "201102"],
  "budget": 300,
  "seed": 1,
  "field": { "gateCount": 9 },
  "system": { "staminaKeepRate": 0.9 },
  "hintLevels": { "201112": 3 },
  "stages": [200, 600, 2000]
}
```

`field` は**出走頭数だけを受け取り、相手の中身はサーバ側で作る**。
任意のプロフィールを渡せるようにすると検査すべき面が一気に広がるうえ、いまの画面もこれしか使っていない。

応答の `result` は `OptimizeResult` がそのまま JSON になったものである。
型付き配列は `Evaluation` の内側にあり、外へは出ない。

個体（`/api/individuals`）は**ステータスとスキル構成だけを持ち、レース結果は持たない**。
レース条件（コース・馬場・出走頭数）はその都度変わるので、勝率の相手にするときも
統計を取るときも、都度ブラウザ側の Worker で再シミュレートする前提である。
サーバ側はステータス範囲とスキル ID の存在を検査して SQLite に置くだけで、
シミュレーションには触れない。

```json
{ "label": "強い先行", "uma": { "charaName": "", "speed": 1200, ... }, "skillIds": ["201112"] }
```

### 7.1 環境変数

| | 既定 | |
| --- | --- | --- |
| `PORT` | 8080 | |
| `RACEEMU_CONCURRENCY` | cgroup から決定 | Worker の本数。明示すると cgroup より優先する |
| `RACEEMU_MAX_RUNNING` | 1 | 同時に走らせるジョブ |
| `RACEEMU_MAX_QUEUED` | 8 | 待ち行列の長さ |
| `RACEEMU_MAX_RACES` | 2,000,000 | 1 ジョブのレース数。超えたら打ち切る |
| `RACEEMU_JOB_TTL_MS` | 30 分 | 終わったジョブを保持する時間 |
| `RACEEMU_STATIC_ROOT` | なし | 静的ファイルの置き場。指定すると同一オリジンで配る |
| `RACEEMU_DATA_DIR` | なし（`:memory:`） | 個体を保存する SQLite の置き場。指定しないとプロセス終了で消える |
| `RACEEMU_TESSDATA` | なし | 文字認識の学習データの置き場。無ければ文字認識には落ちない（分類だけで読む） |
| `RACEEMU_OCR_THRESHOLD` | 170 | 二値化の閾値（[読み取り](ocr-design.md)の 5.2 節） |
| `RACEEMU_MAX_IMAGE_BYTES` | 8 MB | 送れる画像の上限 |

### 7.2 上限の守り方

事前の見積もりでは守らない。
候補数と予算と巡回数から逆算することになり、探索の内部の詰め方に依存してしまう。
**実際に走ったレースを数えて、超えたら中断する。**
`WorkerPool` を継承して `run` の戻りを数えるだけで、探索側には手を入れていない。

## 8. 段取り

1. ~~`apps/api` を作る~~ 済み。
2. ~~イメージとマニフェスト~~ 済み（`deploy/`）。`tsx` は実行時に要るので devDependencies を落とさずに入れている。
3. ~~イメージを置く経路~~ 済み（`.github/workflows/image.yml`）。`main` への取り込みごとに `ghcr.io` を更新する。置き方は[配信](deploy.md)の 5 節にある。
4. **ポッドの中で並列数がどう決まるかを実測する**（[#48](https://github.com/promodeler314-a11y/raceemu/issues/48)）**。** 4.1 の前提の確認である。`GET /api/health` が `concurrencySource` を返すので、`cgroup` と出ていれば読めている。
5. ~~アプリ側に宛先の設定と、投げ先を選ぶ口を足す~~ 済み（[#59](https://github.com/promodeler314-a11y/raceemu/issues/59)）。8.1 節。
6. ~~コミットの SHA をフッタに出す~~ 済み（[#59](https://github.com/promodeler314-a11y/raceemu/issues/59)）。8.2 節。
7. ~~全スキルを候補にできるようにする~~ 済み（[#59](https://github.com/promodeler314-a11y/raceemu/issues/59)）。8.3 節。
   前に置いていた [#58](https://github.com/promodeler314-a11y/raceemu/issues/58)（近似の印）は入った。
   スキルごとの分類は `packages/sim/src/skill/classify.ts` にあり、**レースを回さずに条件式から決まる**。
   そのため口の返す形（4 節のジョブの結果）に印を足す必要は無く、画面が返ってきたスキル ID から自力で解ける。
   候補を数百に広げても、近似で過大評価されているものが上位に並んでいれば表の上で見分けられる。
   理由は[ロードマップ](roadmap.md) 4.3 節にある。

途中で止めてもアプリは壊れない。2 節の前提がそれを保証する。

### 8.1 宛先と投げ先

`apps/web/src/searchApi.ts` が口を叩き、`store.ts` の `runOptimize` が投げ先を選ぶ。
画面は探索の面（`components/ServerSearch.tsx`）にある。

**宛先は空を既定にした。** 同一オリジンを既定にすると、静的配信だけの版が探索のたびに
むだな 1 往復を叩く。空なら今までどおりブラウザで回す。同一オリジンで配っている版では `/` と書く。

**落とすかどうかは「サーバが居るか」で分ける。** ここは二段になっている。

| | 何が起きたか | どうするか |
| --- | --- | --- |
| `ServerSearchUnavailable` | HTML が返った、404/405、宛先に届かない | ブラウザで回す |
| `ServerSearchRejected` | JSON で理由を返してきた（候補が多すぎる、待ち行列がいっぱい、レース数の上限） | 止めて理由を出す |

前者は口が無い版と書き間違いで、今までどおりブラウザで回せば答えが出る。
後者で落とすと**逆に困る**。サーバに投げるほど重い探索（候補 570 個）を黙ってブラウザに回すと、
数十分固まって中断もできない。断られたのであれば、条件を変えるか投げ先を選び直してもらう。

判定は状態番号ではなく**中身が JSON かどうか**で行う。認証や proxy の画面は 200 で HTML を返す。
読み取り（`Import.tsx`）と個体（`individualsApi.ts`）が先にこの形を採っており、そこに揃えた。

**口が受け取らない設定がある。** 相手の束はサーバ側が既定の作り方でしか組まず（7 節）、
自己整合（`selfConsistent`）も口の形に無い。どちらもブラウザで回したときと違う答えになるので、
食い違う設定のときだけ画面が警告を出す。口を広げなかったのは、任意のプロフィールを受け取ると
検査すべき面が一気に広がるのと、**新しい項目を足しても古い版のサーバは黙って無視する**ためである。
版のズレを画面から見分ける手段が無い以上、黙って別物を返すより「反映されない」と言うほうがよい。
残課題は 9 節に挙げた。

### 8.2 コミットの SHA

`vite.config.ts` が `define` で `__COMMIT_SHA__` を埋め、フッタが出す。
ソースへのリンクもその版（`tree/<sha>`）に向ける。
`RACEEMU_COMMIT` か `GITHUB_SHA` があればそちらを先に見て、どちらも無ければ `git rev-parse` を試し、
それも失敗すれば空にする。**版が取れない所で組んでも壊れてはならない**ので、空なら何も出さない。

### 8.3 全スキルの候補化

候補の出どころが 3 通りになった（`store.ts` の `CandidateSource`）。
全スキルは `buildAllSkillCandidates`（`packages/solver/src/candidates.ts`）が組む。

- **入手経路を問わない。** どのデッキで取れるかは別の問いなので、割引も経路の縛りも当てない。
  費用は表示どおりの総額になり、経路の欄は「経路不問」になる。
- **デッキのデータを読まない。** 育成計画は数百 KB のカードと育成ウマ娘のデータを動的 import で
  取るが、こちらは要らない。バンドルも増えない（`skills.json` は前から本体に入っている）。
- 画面の既定（東京 芝2400m・先行・季節と天候と時刻を指定）で **570 個**になる。
  買えるのは白 475 + 金 323 + 継承固有 267 の 1065 個で、そこから
  この設定では発動しようがない 478 個、無視の条件しか持たない 8 個、▲ の 9 個が落ちる。

**▲ を候補から外すつまみを既定で入れた。** ▲ は条件を落としている＝満たしている扱いなので、
発動率も短縮量も本来より高く出る（`classify.ts`）。候補が 20 個のうちは印で見分けられたが、
数百に広げると上位が ▲ で埋まって読めなくなる。#58 のクローズ時に残課題として挙げた点である。

落ちる数は**順位条件を判定しているかどうかで桁が変わる**。順位と距離差の族は、判定すれば ▲ から
△ に上がるためである。東京 芝2400m・先行で測った数は次のとおりで、既定（判定する）では
つまみはほとんど効かない。**効くのは順位条件を切っている人のほうである。**

| | ▲ を入れる | ▲ を外す（既定） |
| --- | ---: | ---: |
| 順位条件を判定する（既定） | 579 個 | 570 個 |
| 判定しない | 579 個 | 259 個 |

△ のほうは外していない。順位条件つきのスキルは継承固有の 8 割 5 分にあたり、外すと
「全スキルを候補にする」ことにならない。印を出したうえで人が読む、という #58 の結論のままである。

**このつまみは育成計画にも効く。** 落とし方は出どころで変えるものではないためで、
育成計画から始めたときの候補も既定で減る（順位条件を切った東京 芝2400m・逃げで 412 個 → 150 個）。
入れ直せば前と同じ候補になる。

**口の候補数の上限を 400 から 1200 に上げた。** 400 のままでは 570 個がそのまま弾かれ、
この段取りの目的を果たせない。枠を守っているのは実際に走ったレース数の見張り
（`RACEEMU_MAX_RACES`、7.2 節）であって候補数ではないので、上限は桁違いの要求を落とすだけの役目にした。

## 9. 残っていること

- **サーバ側で回した探索は、相手の束と自己整合を画面の設定どおりにできない**（8.1 節）。
  口が受け取らないためで、画面は食い違うときに警告を出すにとどまる。
  口を広げるなら、古い版のサーバが新しい項目を黙って無視する問題を先に解く
  （`/api/health` に受け取れる項目を並べる、など）。
- **全スキルの探索をサーバ（ポッド）で実測していない。** 手元で `pnpm optimize --all on` を
  1 本走らせた実測は次のとおりである（4 コア・Worker 3 本、東京 芝2400m・先行、
  予算 600 pt、順位条件あり、▲ を外して候補 565 個。CLI は季節と天候と時刻を指定しないので、
  画面の既定の 570 個とは少し違う）。

  | | |
  | --- | ---: |
  | レース | 269,200 本 |
  | 構成の評価 | 1,221 回 |
  | 所要 | 570 秒 |

  1 節が見積もった「候補 200 個で局所探索 1 巡およそ 16 万レース」と同じ桁である。
  ポッドに載せたときの所要は並列数で決まるので、並列数の実測（段取りの 4、
  [#48](https://github.com/promodeler314-a11y/raceemu/issues/48)）と併せて測る。
- **待っている間の見積もりがサーバ側には無い。** ブラウザで回すときは条件ごとの実測から
  所要を出す（[ロードマップ](roadmap.md) 4.2 節）が、サーバに投げたときは走ったレース数しか出ない。
